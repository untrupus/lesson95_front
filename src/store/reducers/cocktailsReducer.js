import {

} from "../actionTypes";

const initialState = {

};

const cocktailsReducer = (state = initialState, action) => {
    switch(action.type) {

        default:
            return state;
    }
};

export default cocktailsReducer;